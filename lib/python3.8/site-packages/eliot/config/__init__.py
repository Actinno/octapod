"""
Carga la configuración de miniEliot al importar el módulo.
La configuración queda disponible en la variable global 'config'
"""

import sys
from eliot.config.ConfigData import ConfigData
from eliot.logging.basic import log

try:
    config = ConfigData()
    log.debug("Configuración cargada correctamente")
except Exception as e:
    log.error(f"Error cargando configuración: {e}")
    sys.exit(1)

